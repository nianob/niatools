[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "niatools"
version = "1.0.0"
description = "A personal python library by nianob"
authors = [{name="nianob"}]